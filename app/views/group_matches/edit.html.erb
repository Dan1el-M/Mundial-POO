<section class="page-header">
  <div>
    <h1>Edit group match</h1>
    <p>Update teams or the registered result.</p>
  </div>
</section>

<section class="card">
  <%= render "form", match: @match, groups: @groups, teams: @teams %>
</section>
