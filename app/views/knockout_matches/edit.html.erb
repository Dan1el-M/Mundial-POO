<section class="page-header">
  <div>
    <h1>Edit knockout match</h1>
    <p>Regular-time goals are required. Penalties are only needed after a draw.</p>
  </div>
</section>

<section class="card">
  <%= render "form", knockout_match: @knockout_match %>
</section>
